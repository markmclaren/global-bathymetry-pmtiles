services:
  nginx:
    image: nginx:alpine
    ports:
      - "8080:80"
    volumes:
      - ../docs:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./data:/data:ro
    restart: unless-stopped
